// TODO: Implement clamp utility
//
// clamp(value: number, min: number, max: number): number
//   Returns value constrained to the inclusive range [min, max].
//   Used by useRating to prevent out-of-range rating values.
//
// Example:
//   clamp(7, 0, 5) → 5
//   clamp(-1, 0, 5) → 0
//   clamp(3, 0, 5) → 3
