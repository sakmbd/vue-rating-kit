// TODO: Define and export all shared constants used across the library

// CSS class prefix — all component classes must use this prefix (see CLAUDE.md §5)
// export const CSS_PREFIX = 'vrk' as const

// Default number of stars rendered when max prop is not provided
// export const DEFAULT_MAX = 5 as const

// Default initial rating value when modelValue is not provided
// export const DEFAULT_VALUE = 0 as const

// Keyboard keys used for rating navigation
// export const NAVIGATION_KEYS = ['ArrowLeft', 'ArrowRight', 'Home', 'End'] as const
